import React from 'react'

const Carnet = ({ carnet }) => {
  return (
    <div>
    {/* Contenido del componente donde se muestra el carnet */}
    <p>{carnet}</p> {/* Mostrar el valor del carnet */}
  </div>
  )
}

export default Carnet